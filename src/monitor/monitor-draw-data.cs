namespace Expr;

// Marker draw data — monitor has no static rendering config of its own;
// text material/font are borrowed from the Display building's draw data at runtime.
public class MonitorDrawData : IBuildingCustomDrawData { }
